{% set files = field.formattedValue %}
{% if files is not iterable %}
	{% set files = [files] %}
{% endif %}

{% for file in files %}
	{% set html_id = 'ea-lightbox-' ~ field.uniqueId ~ '-' ~ loop.index %}
	<a href="{{ asset(file) }}" title="{{ file }}" target="_blank">{{ file }}</a>
{% endfor %}
